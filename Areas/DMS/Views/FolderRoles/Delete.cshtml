@model iSynergy.Areas.DMS.Models.FolderRight

@{
    ViewBag.Title = "Delete Folder Role";
}

<section class="box" style="border-left: 3px solid #e77512;">
    <header class="panel_header">
        <h2 class="title pull-left">@ViewBag.Title</h2>
    </header>
    <div class="content-body">
        <div class="row">
            <div class="col-xs-12">

                <h3>Are you sure you want to delete this?</h3>
                <div>

                    <dl class="dl-horizontal">
                        <dt>
                            @Html.DisplayNameFor(model => model.DocumentAccessRole.Name)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.DocumentAccessRole.Name)
                        </dd>

                        <dt>
                            @Html.DisplayNameFor(model => model.DocumentFolder.Name)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.DocumentFolder.Name)
                        </dd>

                        <dt>
                            @Html.DisplayNameFor(model => model.hasWriteAccess)
                        </dt>

                        <dd>
                            @Html.DisplayFor(model => model.hasWriteAccess)
                        </dd>

                    </dl>

                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()

                        <div class="form-actions no-color">
                            <input type="submit" value="Delete" class="btn btn-default" /> |
                            @Html.ActionLink("Back to List", "Index")
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</section>


